export const api = 'https://rickandmortyapi.com/api';
export const charactersApi = `${api}/character`;
export const episodeApi = `${api}/character`;
export const locationApi = `${api}/location`;

export const pageLength = 20;
